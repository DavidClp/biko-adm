version: '3.8'

services:
  biko-frontend:
    build: .
    container_name: biko-frontend
    restart: always
    networks:
      - proxy   
    expose:
      - "3000"

networks:
  proxy:
    external: true